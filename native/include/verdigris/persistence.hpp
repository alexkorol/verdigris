#pragma once

// Public include for the deliberately header-only file adapter.  The
// implementation remains under native/persistence so the simulation core has
// no filesystem dependency.
#include "../../persistence/adapter.hpp"
