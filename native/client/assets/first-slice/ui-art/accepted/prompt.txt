Create one 1536x1024 transparent PNG image with image generation 2.5. Set the image-generation background option to transparent. This is a production inventory spritesheet, not concept art. The first attachment is the approved APPEARANCE reference; extract only its clothing, woven footwear and simple branch club. The second attachment is an EXACT PIXEL SIZE AND CANVAS reference: nearest-neighbor enlarged 192x128 logical pixels, 8x8 output pixels per logical pixel. Preserve that coarseness.

Draw exactly four isolated inventory sprites in ONE row, one per equal 384-pixel-wide column, in this order: wooden branch club; male crude flax tunic with its thick rope belt; female shorter sporty asymmetric flax tunic with its thick rope belt; pair of woven fiber shoes with ankle ties. No bodies, heads, mannequins or limbs. No white cloth, colored trim, shoulder capes, bronze armor or decorative metal. All subjects centered at output x=192,576,960,1344. Their logical 48x96 cells run y=16 through111 (128 through895 in output). Keep all objects fully inside each cell with comfortable padding. Club at most76 logical pixels high; tunics at most68 high; footwear at most40 high. All use the SAME coarse 8x8 implied pixel size. Do not enlarge objects to fill the entire image.

This is SIMULATED LOW-RESOLUTION PIXEL ART, gritty realistic classic Diablo2 pre-rendered inventory style. Only broad purposeful clusters of pixels, naturally lit linen/rope/bark/woven fiber, limited subdued warm earth palette. Do not paint subpixel textile detail, photorealistic grain, blurred gradients, or smooth outlines. The pixel-size example controls scale even if the concept image is high resolution.

Transparent background setting is mandatory: real RGBA alpha zero in all blank canvas and gaps. No backdrop, floor, halo, drop shadow, drawn checkerboard, labels, panel borders, or grid lines. Keep real transparency. Generate the image now; no explanations.
