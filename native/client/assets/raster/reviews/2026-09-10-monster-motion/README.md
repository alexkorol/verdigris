# Published-reference monster motion and native server pursuit

Root accepts the exact four SE raider and eight cleaned SW wight PNGs as
provisional production integration. All 162 earlier runtime PNGs are unchanged.
The current catalog contains174 sprites,31 manifests,60 sources and183 import
records. This milestone remains part of the active visual/gameplay goal.

The research is in `../../RESEARCH.md`; adapted prompting rules are in
`../../PROMPTING.md`. The SE trial used an accepted hero pose as geometry and
the raider idle as identity, adapting Higgsfield's published guide roles.
Five built-in calls included one axe repair. The wight starts from PURESO's
published walking-sheet recipe; two pose repairs were rejected. Broad gray
background cleanup erased pale hand pixels, so selected cleanup preserves
source RGB and limits removal to reviewed background regions. Source-owner
records retain their original candidate acceptance context; this review is
the later production decision.

Root viewed the native candidate strips, every SE phase in actual pursuit,
seven SW wight phases in a real WebSocket crypt session, contact, settled idle
and replacement scene. `raider-se-production-crops-1x.png` and
`wight-sw-production-crops-1x.png` are exact unscaled crops of the retained full
screenshots. They demonstrate selected phases, not chronological playback.
The real crypt trace captures seven of eight poses; phase4 was not observed
during that short chase and is verified by the exhaustive raster-world check.

Normal `WorldSimulation` melee now pursues through its ordinary server tick,
with continuous endpoints, deterministic navigation/body separation, bounded
acquisition/leash/catchup and warning/recovery locks. Remote interpolation
changes display coordinates only. Ordinary hits retain the actor's rarity,
scale and last authoritative facing. The prior local `Simulation` pursuit
milestone remains separate; no JavaScript server or second local simulation
substituted for this real-server review.

Final supported native build and every native suite/all72 scenarios pass.
Browser32/32, importer10/10 and equipment60x5x3 plus fractional sampling pass.
Twenty3440x1440 frames average22.510 ms static and24.684 ms moving, peak33.976
ms; both40 ms average limits remain unchanged. The initial combined compile
failed because the new review fixture called a private lookup helper; root
replaced it with a fixture-local lookup. The subsequent full supported run is
green. Original failure and final logs are retained.

Root launched the normal supported native client/server, used F to found a
House, C to create a Scion,1 to enter town, N for expedition entry, and F3 for
the live overlay. The live window stayed at100life at the entry and reported
18.3 ms paint. A short D tap did not produce visible displacement. Esc closed
the session with exit0; launcher PID checks and a fresh native window inventory
confirmed no orphan processes. Manual combat completion is unverified; actual
pursuit/damage/stop proof comes from the production scenario.

Remaining visible limits: raider body/mask/axe drift; narrower wight torso and
cloth-obscured hip ownership; no authored wight attack; unchanged tile-based
contact range can read long against short arms. Normal dungeon/crypt ground is
still noisy and wall cells remain flat placeholders. F3 overlays overlap other
HUD text. These screenshots do not establish final art, smooth video, complete
monster coverage or a completed manual gameplay loop.

`root-review.json` records exact validation/source hashes. `retained-evidence.json`
records crop rectangles and original screenshot hashes. No commit/push/merge
or goal-completion outcome is inferred from image generation alone.
