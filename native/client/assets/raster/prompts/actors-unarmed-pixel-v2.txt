Use case: style-transfer
Asset type: TRUE LOW-RESOLUTION PIXEL ART production character sprite sheet for a pixel-art game.
Input image 1 is only the character-identity, pose, and cell-order reference. Rebuild the sheet as authentic low-resolution pixel art. Do not retain the detailed painted rendering.

Primary request: A square transparent PNG containing exactly 4 columns x 4 rows of isolated full-body adult sprites. The same 16 body poses and same row/column order as the reference: hero ready SE/SW/NW/NE; hero attack SE/SW/NW/NE; masked raider ready SE/SW/NW/NE; masked raider attack SE/SW/NW/NE. Hero in TOP TWO ROWS is entirely EMPTY-HANDED with curled equipment-grip hands. Raider in BOTTOM TWO ROWS keeps its bronze axe. The two actors retain their original reference identity, clothes and palettes.

CRITICAL PIXEL CONSTRUCTION: Imagine drawing the entire square sheet on a 384 x 384 logical pixel canvas, split into sixteen 96 x 96 logical-pixel cells, and then displaying it enlarged using strictly NEAREST NEIGHBOR scaling. EACH CHARACTER is only about 38 to 48 logical pixels wide and 72 to 78 logical pixels tall. Every single visible color mark must be a hard-edged square logical pixel or a contiguous cluster of such pixels on ONE common grid. The large square pixel steps must be immediately obvious at the full sheet size. No fine image detail smaller than a logical pixel. This is deliberately low-resolution GAME SPRITE ART, not a painting with a pixel filter, not a high-resolution illustration, not realistic textured raster, not smooth lineart. Use at most 24 opaque colors across the sheet. Use 3 to 4 flat shades per material, broad connected areas, absolutely NO smooth shading, antialiasing, dithering, speckles, noise, microtexture or gradient. Faces use only a few pixels for eyes, brow and nose, never detailed facial rendering. Linen folds are blocky two-tone shapes. Bronze is a few clean gold/brown/verdigris clusters. Edges use characteristic large 1-pixel staircase diagonals.

Character constraints: mature adult seven-head-tall bodies, small heads, practical lean builds. The hero's muted russet linen, dark leather, bronze shoulder guard and blue-green sash remain recognizable in every view. The masked raider's charcoal tunic, ochre sash, dark hair, small bone mask and short bronze axe remain recognizable. No steel, swords, shields, helmets, oversized armor or chibi proportions.

Technical composition: evenly spaced 4x4 layout, equal square cells, each full-body sprite contained entirely inside one cell, centered with feet near the bottom of its own cell and generous transparent margins. The same anatomical scale across all sprites. Slightly elevated isometric camera, warm upper-left light expressed ONLY as discrete flat pixel clusters. Back-facing NW/NE sprites show backs of heads, no face or mask peeking around.

Transparency: genuine RGBA with fully transparent empty pixels and fully opaque colored sprite pixels; binary alpha only. No fake checkerboard. No backdrop, no drop shadow, no platform, no grid lines, no text. Clean isolated hard-edged sprites ready to slice directly into a pixel-art game.
