Use case: precise-object-edit.
Edit this environment sprite atlas only to fix its production layout. Preserve exactly these same twelve objects, palette, materials, detailed original pixel-art style, isometric view, and row-major object order. Keep every object looking the same.
Important: the current objects are too large for their cells and run into the sheet edges and across equal cell boundaries. The corrected image must have exactly 4 columns and 3 rows of EQUAL SQUARE CELLS, rectangular 4:3 canvas. In every cell shrink its existing object to fit comfortably inside a central safe box occupying 78% of cell width and 78% of cell height. At least 10% empty transparent margins on every side inside each cell. Leave larger gap between rows so absolutely no object crosses an imaginary equal cell boundary. No part touches the outer image edges. Every full tree branch, tree canopy, building roof, brazier flame and storage item stays fully visible inside its own cell. All object ground-contact points at 88% down their respective equal cell; center objects horizontally in cells.
Exact objects and ordering remain: row1 gnarled tree, dead tree, rock outcrop, fernclump; row2 mudbrick thatchhut, timber storehut, broken column, verdigris altar; row3 bronze-banded closed wooden chest, burning brazier, sack-and-pottery cluster, broken stone wall.
Keep genuine alpha transparency for all empty areas. No black background, no painted checkerboard, no grid, no labels, no extra objects. This is a layout correction, preserve the existing sprites' excellent style.
