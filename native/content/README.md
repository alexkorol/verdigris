# Content seam

The first slice uses explicit placeholder data. A future content layer can load
versioned actors, items, routes, and seasonal definitions while keeping content
data separate from simulation algorithms.
