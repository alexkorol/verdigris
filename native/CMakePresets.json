{
  "version": 2,
  "configurePresets": [
    {
      "name": "default",
      "displayName": "Verdigris native (default)",
      "generator": "Ninja",
      "binaryDir": "${sourceDir}/build/cmake/${presetName}",
      "cacheVariables": {
        "CMAKE_BUILD_TYPE": "Debug"
      }
    },
    {
      "name": "windows-msvc",
      "displayName": "Verdigris native (MSVC)",
      "description": "Version-neutral NMake preset; schema v2 requires an explicit generator, so the workflow supplies the MSVC developer environment instead of pinning a VS edition.",
      "generator": "NMake Makefiles",
      "binaryDir": "${sourceDir}/build/cmake/${presetName}"
    }
  ],
  "buildPresets": [
    {
      "name": "default",
      "configurePreset": "default"
    },
    {
      "name": "windows-msvc",
      "configurePreset": "windows-msvc",
      "configuration": "Release"
    }
  ],
  "testPresets": [
    {
      "name": "default",
      "configurePreset": "default",
      "output": {
        "outputOnFailure": true
      }
    },
    {
      "name": "windows-msvc",
      "configurePreset": "windows-msvc",
      "configuration": "Release",
      "output": {
        "outputOnFailure": true
      }
    }
  ]
}
