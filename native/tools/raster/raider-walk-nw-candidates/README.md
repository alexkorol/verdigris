# NW raider walk candidate

Frozen September 10, 2026. Root provisionally approved eight frames for a production trial; actual game acceptance remains with root.

`raider-walk-nw-candidate.json` reconstructs the selected v2 source through the existing shared importer and the actual external Pixel Respecter workspace API. It produces `selected/raider_walk0_nw.png` through `raider_walk7_nw.png`. All frames use an 80×96 canvas, pivot40,96, common source pitch6, and the accepted NW idle palette. Thirty of that palette's32 colors are used across the cycle. Alpha is binary. Body heights54–59px compare with idle56px. No frame is independently fitted or mirrored.

`selected/native-1x.png` and `selected/identity-3x.png` show the final pixels beside idle. `selected/walk-3x.gif` and `selected/playback.html` provide an880ms loop. Ordered native review finds opposite support in the second half, passing/weight shifts, a stable back view, and the bronze axe remaining in the anatomical-right/screen-right hand. Contact phases0/1 and3/4/5 are relatively close. The smaller axe and remaining crown offset are material limitations, explicitly accepted only for the provisional trial.

The initial single-reference sheet repeated its supporting leg. The focused repair assigned an existing validated NW D2 pose sheet as the body-pose edit target and the accepted raider as appearance reference. This follows our earlier SW production adaptation; it is not a published creator-tested NW gait recipe. The external raw pose reference remains outside the repository. The further alpha-only repair failed, so v2 remains selected.

V2 has an opaque painted background. Cleanup uses the owner's border-connected color-fill API, with12 tiny, source-reviewed windows for enclosed hair and elbow holes. `cleanup-windows-source-audit.png` shows each exact window. Gray cloak highlights outside those holes were preserved. No claim of generated true alpha is made for v2 or v3.

`raider-walk-nw-dependencies.json` contains the exact minimal import dependencies, frozen frame hashes, external-reference hash, provenance path, import command, geometry and limitations. Full prompts/reference roles/rejections are in `native/client/assets/raster/prompts/raider-walk-nw-provenance.json` relative to the repository. The selected importer report records actual external engine and Python/library hashes/versions.

This lane changed no runtime, catalog, shared importer or game code. Candidate playback and strip inspection are separate from the production renderer, enemy movement and full gameplay gates.
