# Networking seam

No production networking is in this sprint. A future transport can carry
commands to an authoritative simulation and events/snapshots back to clients.
The core remains socket-free so headless deterministic tests and a dedicated
server can share it.
