# Persistence seam

No database is required for the first proof. A future versioned serializer will
persist House state, Scion history, item identity/history, and campaign graph
ownership without putting SQLite or filesystem calls in the simulation core.
